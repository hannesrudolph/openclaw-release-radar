// Score-history charts and user-filtered trend data need the same release window.
// Keep this independent of RELEASES_LIMIT, which controls how many rows the main
// release list displays.
export const SCORE_HISTORY_CHART_LIMIT = 20;
