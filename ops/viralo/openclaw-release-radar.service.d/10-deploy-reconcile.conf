[Unit]
# Kept as a separately hashed deployment-control artifact. The authoritative
# application unit carries the same ordering so either file alone is explicit.
Requires=openclaw-release-radar-reconcile-boot.service
After=openclaw-release-radar-reconcile-boot.service
