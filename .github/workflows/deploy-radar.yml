name: Deploy Release Radar

on:
  push:
    branches:
      - main
  workflow_dispatch:

concurrency:
  group: deploy-release-radar
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      contents: read
    env:
      RELEASE_NAME: release-${{ github.sha }}
      RELEASE_TARBALL: openclaw-release-radar.tar.gz
      HEALTHCHECK_URL: ${{ vars.DEPLOY_HEALTH_URL || secrets.DEPLOY_HEALTH_URL }}
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Verify app
        run: npm run verify:ci

      - name: Package release artifact
        run: |
          tar -czf "$RELEASE_TARBALL" \
            dist \
            public \
            package.json \
            package-lock.json \
            README.md

      - name: Configure SSH
        run: |
          install -d -m 700 ~/.ssh
          printf '%s\n' "${{ secrets.DEPLOY_SSH_KEY }}" > ~/.ssh/id_ed25519
          chmod 600 ~/.ssh/id_ed25519
          ssh-keyscan -p "${{ secrets.DEPLOY_SSH_PORT }}" -H "${{ secrets.DEPLOY_SSH_HOST }}" >> ~/.ssh/known_hosts

      - name: Upload release tarball
        run: |
          scp -P "${{ secrets.DEPLOY_SSH_PORT }}" \
            "$RELEASE_TARBALL" \
            "${{ secrets.DEPLOY_SSH_USER }}@${{ secrets.DEPLOY_SSH_HOST }}:/tmp/$RELEASE_NAME.tar.gz"

      - name: Install release on server
        run: |
          ssh -p "${{ secrets.DEPLOY_SSH_PORT }}" \
            "${{ secrets.DEPLOY_SSH_USER }}@${{ secrets.DEPLOY_SSH_HOST }}" \
            "sudo /usr/local/bin/openclaw-release-radar-install-release /tmp/$RELEASE_NAME.tar.gz $RELEASE_NAME"

      - name: Verify public health endpoint
        run: |
          : "${HEALTHCHECK_URL:?Set DEPLOY_HEALTH_URL to https://your-domain.example/api/health}"
          for _ in $(seq 1 20); do
            if curl -fsS "$HEALTHCHECK_URL"; then
              exit 0
            fi
            sleep 3
          done
          exit 1
